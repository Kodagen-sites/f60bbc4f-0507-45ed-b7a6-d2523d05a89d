// Re-export so both `@/content/site-config` and `@/lib/site-config` resolve to
// the single canonical config object.
export { siteConfig } from "@/content/site-config";
export type { SiteConfig } from "@/content/site-config";
